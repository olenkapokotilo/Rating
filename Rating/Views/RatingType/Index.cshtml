@model IEnumerable<Rating.Models.RatingTypeModel>
<h2>RatingType List</h2>
    <table>
            @foreach (var rt in Model)
            {
                <tr>
                    <td>@rt.Name   <a href="/RatingType/Edit/@rt.Id">Edit</a>  <a href="/RatingType/Delete/@rt.Id">Delete</a></td>
                </tr>
            }
            <tr>
                <td><a href="/RatingType/Create"> + </a></td>
            </tr>
    </table>
