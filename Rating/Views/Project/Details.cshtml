@model Rating.Models.ProjectModel
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="col-md">
    <h2>Details for @Model.Name</h2>
    <h3>RatingTypes</h3>
    <table class="table table-hover">
@foreach (var rt in Model.RatingTypes)
        {
        <tr>
            <td><p>@rt.Name</p></td>
            <td></td>
        </tr>
        @*@foreach (var at in rt.ActionTypes)
        {
            <tr>
                <td><p>@at.Name    </p></td>
                <td><p>@at.Scores   </p></td>*@
                @*<td><a href="/ActionType/Edit/@at">  Edit</a></td>*@
            @*</tr>*@
        }
    <tr>
        @*<td><a href="/RatingType/Edit/@rt">  Edit</a></td>*@
    </tr>
        }
    </table>
</div>

